# Listing Enhancer

This is an iterative file that will be updated at the end of each coding session.

# Added downloader and root directory utility

Today, I implemented a downloader to retrive the latest cohort of data. To improve portability across different environments, I also added a utility that dynamically locates the project root by searching for the .git directory. This ensures consistent and realiable fiel path resolution.

The next step is to extract the most recent information from the data dictionary and begin the data cleaning process.